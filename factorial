/* Recursive approach to solve factorial of a number */
function factorial(num) {
  if (num < 0) 
        return -1;
  else if (num == 0) 
      return 1;
  else {
      return (num * factorial(num - 1));
  }
}
/* Sample input 5, output should be 120 (5*4*3*2*1) */
console.log(factorial(5));